{
  "name": "blackjack-practice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Local blackjack practice with basic-strategy coach. Run `npm run dev`, start Ollama (default http://127.0.0.1:11434), optional: LLM_PROXY_TARGET for LM Studio.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "~5.7.2",
    "vite": "^6.0.3",
    "vitest": "^3.0.0"
  }
}
